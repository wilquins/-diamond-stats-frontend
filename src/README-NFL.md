# NFL — reservado para el futuro

Esta carpeta está vacía a propósito. Cuando se construya la sección de
NFL, todo su código (componente principal, lógica de datos, etc.) va a
vivir aquí, siguiendo el mismo patrón que `/mlb`.

Por ahora, la app solo tiene MLB funcionando.
